# 1. Базовый образ
FROM python:3.13

# 2. Создание рабочей директории
WORKDIR /app

# 4. Копирование зависимостей
COPY requirements.txt .

# 5. Установка зависимостей
# Флаг `--no-cache-dir` отключает кэширование, чтобы уменьшить размер финального образа.
RUN pip install --no-cache-dir -r requirements.txt

# 6. Копирование исходного кода
# Копируем весь исходный код проекта внутрь контейнера.
COPY . .

# 7. Команда для запуска приложения
CMD ["python", "main.py"]